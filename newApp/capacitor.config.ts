import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miPrimeraApp.app',
  appName: 'miPrimeraApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
